function solution(box, n) {
    var answer = 0;
    let boxVolume = parseInt(box[0] / n) * parseInt(box[1] / n) * parseInt(box[2] / n);
    let diceVolume = Math.pow(n, 3);
    
    
    
    return boxVolume;
}